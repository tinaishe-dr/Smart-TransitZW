{
  "name": "smarttransitzw-security-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase --config ../../firebase.json emulators:exec --project demo-smarttransitzw --only firestore \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  }
}
